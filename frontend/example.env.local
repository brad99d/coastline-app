# This is an example environment file
# Create a duplicate without the "example" prefix and add your own secrets.

# For client-side requests
NEXT_PUBLIC_API_URL= # typically, localhost:8000

# For server-side requests
API_URL= # typically, backend:8000